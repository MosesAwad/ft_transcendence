fetch("http://localhost:3000/login", {
  method: "POST",
  credentials: "include", // crucial for receiving cookies
  headers: {
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    username: "mr11@gmail.com",
    password: "habe234",
    deviceId: crypto.randomUUID() // generates a fresh UUID
  })
});
